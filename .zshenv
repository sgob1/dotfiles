# -----------------------------------------------------------------------------
# GNOME keyring startup
# -----------------------------------------------------------------------------
if [ -n "$DESKTOP_SESSION" ];then
    eval $(gnome-keyring-daemon --daemonize --start 2> /dev/null) > /dev/null
    export SSH_AUTH_SOCK
fi
# -----------------------------------------------------------------------------
